language: php

php:
  - 7.1

#stop travis mysql
before_install:
  - sudo service mysql stop

script:
  - php --version

deploy:
  provider: script
  script: bash trigger-deploy.sh master $TRAVIS_TOKEN
  on:
    branch: master

